- name: Install Packages
  hosts: local
  connection: local
  because: yes
  tasks:
  - name: Print names
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
  loop:
  - python3
  - nginx
  - mysql